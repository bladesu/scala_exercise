Here are exercises for practice scala lang. I put the implementation in component package and  test case in test folder which leveraging the scalatest framework.

To run all the test:

```dtd
./gradlew test
```